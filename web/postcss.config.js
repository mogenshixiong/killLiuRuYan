export default {
  plugins: {
    autoprefixer: {},
    tailwindcss: {}
    // eslint-disable-next-line no-undef
    // ...(process.env.NODE_ENV === 'production' ? { cssnano: {} } : {})
  }
}
